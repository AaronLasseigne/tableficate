require 'spec_helper'

describe Tableficate::Helper do
  describe '#tableficate_filter_form_for(table)' do
    # it 'creates a filter form based on the information provided to Tableficate::Table' do
    #   table = Tableficate::Table.new(nil, NobelPrizeWinner.tableficate({}).limit(1), {show_sorts: true}, {})
    #   table.filter(:first_name)

    #   output = helper.tableficate_filter_form_for(table)

    #   output.should have_selector('label[text()="First Name"]')
    #   output.should have_selector('input[type!="submit"]')
    # end
  end
end
